---
- name: run tests
  hosts: all
  gather_facts: no
  sudo: yes
  tasks:
    - name: check for docker binary in path
      shell: command -v docker
      changed_when: false

    - name: check for docker0 bridge
      command: ip a s docker0
      changed_when: false
